version: '3.8'
services:
  bot:
    build: .
    container_name: bb-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DISCORD_TOKEN=
      - RCON_HOST=
      - RCON_PORT=
      - RCON_PASSWORD=
